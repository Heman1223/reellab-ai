[pytest]
# Run pytest from the `ai/` directory. `pythonpath` makes the service packages
# (schemas, audience, personas, ...) importable without an install step.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
